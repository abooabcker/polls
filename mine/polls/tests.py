from django.test import TestCase
#from django.utils import timezone
#from .models import Question
#import datetime
#import pytest
#@pytest.mark.django_db
#def test_future_pubished():
   # time = timezone.now() + datetime.timedelta(days=30)
   # future_question= Question(question_text="why",pu_date=time)
    #future_question.save()
   # assert future_question.was_pulished_rescently()==False
#class QuestionMethodTest(TestCase):
  
 #   def test_was_published_recently_with_old_question(self):
  #      time = timezone.now() - datetime.timedelta(days=30)
   #     old_question = Question(pu_date=time)
    #    self.assertIs(old_question.was_pulished_rescently(), False)

 #   def test_was_published_recently_with_recent_question(self):
  #      time = timezone.now() - datetime.timedelta(hours=1)
   #     recent_question = Question(pu_date=time)
    #    self.assertIs(recent_question.was_pulished_rescently(), True)


        
